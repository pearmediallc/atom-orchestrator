[pytest]
testpaths = tests
addopts = -ra -q --strict-markers
markers =
    integration: tests that require an external service (e.g. ATOM running on :5500)
    uses_real_db: tests that opt out of the conftest production-DB isolation guard. Should be very rare — almost everything works against tmp_inventory's SQLite.
